<p class="text-success bg-success"><%= notice %></p>
<h1><%= t('dictionary.words.admin') %></h1>
<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <% @users.each do|user| %>
        <td><%= user.user_name %>&nbsp;</td>
        <td><%= user.tasks.count %></td>
        <td><%= link_to t('dictionary.words.details'), admin_user_path(user.id), class: 'btn btn-worning button' %></td>
        <td><%= link_to t('dictionary.words.edit'), edit_admin_user_path(user.id), class: 'btn btn-worning button' %></td>
        <td><%= link_to t('dictionary.words.delete'), admin_user_path(user.id), method: :delete, class: 'btn btn-worning button' %></td>
      <% end %>
    </tr>
  </tbody>
</table>
